{
  "name": "package.json",
  "version": "1.0.0",
  "description": "This is a simple website that generates a random number \r for user to get a random health based fact The random number must not be multiple by 13 else You win the lucky competition\r and will show a winning or lossing message.user don't need to do anything",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
    , "start": "nodemon router.js"
    
  },
  "author": "gv",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "mongoose": "^8.1.1"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  }
}
